Projeto alura books responsivo usando mobile-first.
Nesse projeto foi utilizado:
abordagem mobile first; flex-items;import de fontes; menu sanduiche; carrossel com plugin externo swiperJS através de CDN; media query;
